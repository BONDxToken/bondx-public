(mod (BONDX_GENESIS_ID
      MAX_SUPPLY
      DECIMALS
      EPOCH_SECONDS
      EPOCH_OFFSET_UTC
      MIN_UNIT)
  (q . (BONDX_GENESIS_ID MAX_SUPPLY DECIMALS EPOCH_SECONDS EPOCH_OFFSET_UTC MIN_UNIT))
)
